 {% extends 'monTemplate.html.twig' %}

{% block title %}ProStage - Formations {% endblock %}

{% block contenu %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>
<div class="example-wrapper">
 <h2>Liste des formations pour lesquelles un stage est proposé</h2>
 </div>

 <table class="table">
    <thead class="thead-dark">
      <tr>
        <th scope="col">Formation</th>
         <th scope="col">Détails</th>
      </thead>
         <tbody>
            
          
          {% for formationCourante in formations %}
          <tr>
              <td>{{formationCourante.nomCourt}}</td>            
              <td> <a href = "{{ path('pro_stage_formation_id', {'id': formationCourante.id}) }}"> Liste des stages</a></td>
            </tr>
            <tr>
           {% endfor%}

    

            </tbody>
      </table>
{% endblock %}